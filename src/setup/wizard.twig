<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ pageConfig.TITLE }} | AdamRMS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body class="bg-light">
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h1 class="h3 mb-3">Welcome to AdamRMS</h1>
                    <p class="text-muted">Create the first administrator account to finish the initial setup.</p>
                    {% if errors.general %}
                        <div class="alert alert-danger" role="alert">
                            {{ errors.general }}
                        </div>
                    {% endif %}
                    <form method="post" novalidate>
                        <div class="mb-3">
                            <label for="setup-name1" class="form-label">First name</label>
                            <input type="text" class="form-control {{ errors.name1 ? 'is-invalid' : '' }}" id="setup-name1" name="name1" value="{{ values.name1|e }}" required>
                            {% if errors.name1 %}<div class="invalid-feedback">{{ errors.name1 }}</div>{% endif %}
                        </div>
                        <div class="mb-3">
                            <label for="setup-name2" class="form-label">Last name</label>
                            <input type="text" class="form-control {{ errors.name2 ? 'is-invalid' : '' }}" id="setup-name2" name="name2" value="{{ values.name2|e }}" required>
                            {% if errors.name2 %}<div class="invalid-feedback">{{ errors.name2 }}</div>{% endif %}
                        </div>
                        <div class="mb-3">
                            <label for="setup-email" class="form-label">Email address</label>
                            <input type="email" class="form-control {{ errors.email ? 'is-invalid' : '' }}" id="setup-email" name="email" value="{{ values.email|e }}" required autocomplete="email">
                            {% if errors.email %}<div class="invalid-feedback">{{ errors.email }}</div>{% endif %}
                        </div>
                        <div class="mb-3">
                            <label for="setup-username" class="form-label">Username</label>
                            <input type="text" class="form-control {{ errors.username ? 'is-invalid' : '' }}" id="setup-username" name="username" value="{{ values.username|e }}" required autocomplete="username">
                            {% if errors.username %}<div class="invalid-feedback">{{ errors.username }}</div>{% endif %}
                            <div class="form-text">Allowed characters: letters, numbers, dots, hyphens and underscores.</div>
                        </div>
                        <div class="mb-3">
                            <label for="setup-password" class="form-label">Password</label>
                            <input type="password" class="form-control {{ errors.password ? 'is-invalid' : '' }}" id="setup-password" name="password" required autocomplete="new-password" minlength="{{ passwordMinimumLength }}">
                            {% if errors.password %}<div class="invalid-feedback">{{ errors.password }}</div>{% else %}<div class="form-text">Must be at least {{ passwordMinimumLength }} characters.</div>{% endif %}
                        </div>
                        <div class="mb-4">
                            <label for="setup-password-confirm" class="form-label">Confirm password</label>
                            <input type="password" class="form-control {{ errors.password_confirm ? 'is-invalid' : '' }}" id="setup-password-confirm" name="password_confirm" required autocomplete="new-password" minlength="{{ passwordMinimumLength }}">
                            {% if errors.password_confirm %}<div class="invalid-feedback">{{ errors.password_confirm }}</div>{% endif %}
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary">Create administrator</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>
